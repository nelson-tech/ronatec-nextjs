import styled from "@emotion/styled"
import tw from "twin.macro"

export const MobileMenuPane = styled.div`
  max-width: 350px;
  // Quick edits below
  ${tw`relative w-full bg-white shadow-xl pb-12 flex flex-col overflow-y-auto`}
`
