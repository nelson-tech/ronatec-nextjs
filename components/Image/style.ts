// import styled from "@emotion/styled"
// import tw from "twin.macro"

type ImageContainerProps = {
  rounded?: "sm" | "md" | "lg"
}

export const ImageContainer = null

// styled.div<ImageContainerProps>`
//   border-radius: ${({ rounded }) =>
//     rounded
//       ? rounded === "sm"
//         ? "0.125rem"
//         : rounded === "md"
//         ? "0.25rem"
//         : rounded === "lg"
//         ? "0.5rem"
//         : "0"
//       : "0"};
//   overflow: ${({ rounded }) => (rounded ? "hidden" : "")};

//   // Quick edits below
//   ${tw`relative`}
// `
